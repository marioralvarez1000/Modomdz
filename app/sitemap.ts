import type { MetadataRoute } from "next";import { content,hrefFor } from "@/lib/content";const base="https://modomza.com.ar";export default function sitemap():MetadataRoute.Sitemap{const fixed=["","/hoy","/gratis","/agenda","/lugares","/itinerarios","/guias","/enviar-evento","/anunciar","/contacto","/privacidad","/terminos"].map(path=>({url:`${base}${path}`,lastModified:new Date("2026-09-11")}));return [...fixed,...content.map(item=>({url:`${base}${hrefFor(item)}`,lastModified:new Date("2026-09-11")}))]}
