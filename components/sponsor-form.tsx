"use client";
import { FormEvent,useState } from "react";import { CheckCircle2 } from "lucide-react";import { track } from "@/lib/track-client";
export function SponsorForm(){const [status,setStatus]=useState<"idle"|"loading"|"ok"|"error">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("loading");const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/sponsor",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(res.ok){setStatus("ok");track("sponsor_form_submit");}else setStatus("error");}if(status==="ok")return <div className="simple-card"><CheckCircle2/><h2>Recibimos tus datos</h2><p>Te contactaremos para conocer la propuesta y evaluar su participación.</p></div>;return <form className="simple-card" onSubmit={submit}><h2>Contanos sobre tu negocio</h2><label>Tu nombre</label><input name="name" required maxLength={80}/><label>Local, marca o proyecto</label><input name="business" required maxLength={120}/><label>Email</label><input name="email" type="email" required maxLength={160}/><label>Teléfono (opcional)</label><input name="phone" maxLength={40}/><label>Categoría</label><select name="category"><option>Gastronomía</option><option>Alojamiento</option><option>Experiencias</option><option>Comercio</option><option>Transporte</option><option>Otro</option></select><label>Contanos brevemente qué ofrecés</label><textarea name="message" maxLength={1000}/><label className="consent-check"><input type="checkbox" name="consent" value="yes" required/> Acepto que Modo MZA use estos datos para responder mi consulta, según la <a href="/privacidad">política de privacidad</a>.</label><button disabled={status==="loading"}>{status==="loading"?"Enviando…":"Enviar interés"}</button>{status==="error"&&<p>No pudimos enviarlo. Revisá los campos e intentá nuevamente.</p>}<p><small>El envío no garantiza publicación. Toda inclusión comercial se identificará claramente.</small></p></form>}
