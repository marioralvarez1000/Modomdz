"use client";
import { FormEvent,useState } from "react";import { ArrowRight,Check } from "lucide-react";import { track } from "@/lib/track-client";
export function NewsletterForm(){const [status,setStatus]=useState<"idle"|"loading"|"ok"|"error">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("loading");const form=new FormData(e.currentTarget);const response=await fetch("/api/newsletter",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:form.get("email"),consent:"yes"})});if(response.ok){setStatus("ok");track("newsletter_signup")}else setStatus("error")}if(status==="ok")return <p className="form-success"><Check/>Listo. Te sumamos a la próxima agenda.</p>;return <form className="newsletter-form" onSubmit={submit}><div><input required type="email" name="email" placeholder="tu@email.com" aria-label="Tu email"/><button disabled={status==="loading"}>{status==="loading"?"Enviando…":<>Mandame los planes <ArrowRight size={17}/></>}</button></div><label><input type="checkbox" required/>Acepto recibir la agenda por email y la <a href="/privacidad">política de privacidad</a>. Puedo cancelar cuando quiera.</label>{status==="error"&&<small>No pudimos guardarlo. Revisá el email e intentá otra vez.</small>}</form>}
